# 斐波那契堆 (Fibonacci Heap)

> 📘 _算法导论第19章学习指南_

## 🎯 1. 简介

**斐波那契堆**(Fibonacci Heap)是一种高效的数据结构，主要用于实现优先队列。它是由Michael L. Fredman和Robert E. Tarjan在1984年开发的，并在1987年发表。斐波那契堆的名字来源于其分析中使用的斐波那契数列。

斐波那契堆的主要优势在于其优秀的摊还时间复杂度，特别是在涉及大量插入和合并操作的应用中，比如图算法中的Dijkstra算法和Prim算法。

### 1.1 为什么需要斐波那契堆？

在许多算法中，我们需要频繁地执行以下操作：
1. 插入元素
2. 查找最小元素
3. 合并两个优先队列
4. 减小元素的键值

传统的二叉堆在合并操作上的时间复杂度为O(n)，而斐波那契堆通过延迟工作的方式，将合并操作的时间复杂度降低到O(1)。

让我们通过一个具体场景来理解这个问题：

假设我们要实现Dijkstra算法来寻找图中从源点到各顶点的最短路径：
- 需要频繁地插入新发现的顶点（INSERT操作）
- 需要频繁地合并不同的顶点集合（UNION操作）
- 需要反复抽取当前距离最小的顶点（EXTRACT-MIN操作）
- 需要不断更新顶点的距离估计值（DECREASE-KEY操作）

在传统二叉堆中，UNION操作需要O(n)时间，这会导致整个算法效率低下。而斐波那契堆的UNION操作只需要O(1)时间，大大提高了算法效率。

### 1.2 斐波那契堆的特点

1. **懒惰操作**：许多工作被推迟到真正需要时才执行
2. **摊还效率**：虽然单次操作可能较慢，但多次操作的平均时间复杂度很好
3. **理论最优**：在理论上达到了很多操作的下界

### 1.3 斐波那契堆与斐波那契数的关系

斐波那契堆之所以得名，是因为在其时间复杂度分析中使用了斐波那契数列。具体来说，一个度数为k的节点的子节点的度数序列恰好是斐波那契数列：0, 1, 1, 2, 3, 5, 8, 13, ...

这为我们提供了度数上界的理论基础，即度数为k的节点至少包含F_{k+2}个后代节点，其中F_k是第k个斐波那契数。

## 📚 2. 斐波那契堆的结构

### 2.1 基本概念

斐波那契堆是一组最小堆有序树的集合，满足以下性质：

1. **最小堆性质**：每个节点的关键字不大于其子女的关键字
2. **根列表**：堆中所有树的根节点构成一个双向循环链表
3. **指针指向**：堆对象H有一个指针min[H]指向根列表中关键字最小的节点

### 2.2 节点结构详解

每个节点包含以下字段：

```
struct FibonacciNode {
    int key;                         // 节点的关键字
    int degree;                      // 节点的度数（子节点数量）
    bool mark;                       // 标记位，用于删除操作
    FibonacciNode* parent;           // 指向父节点的指针
    FibonacciNode* child;            // 指向某个子节点的指针
    FibonacciNode* left;             // 指向左兄弟节点的指针
    FibonacciNode* right;            // 指向右兄弟节点的指针
};
```

让我们详细解释每个字段的作用：

1. **key**：节点存储的关键字值，用于比较大小
2. **degree**：节点的度数，即直接子节点的数量
3. **mark**：标记位，当节点失去一个子节点时被标记为true
4. **parent**：指向父节点的指针，根节点的parent为nullptr
5. **child**：指向其中一个子节点的指针（子节点通过left/right链接形成循环链表）
6. **left, right**：指向兄弟节点的指针，形成双向循环链表

### 2.3 双向循环链表结构

斐波那契堆大量使用双向循环链表来组织节点：

```
根列表结构示例：
min -> [23] <-> [7] <-> [3] <-> [18] <-+
        ^                               |
        +-------------------------------+

子节点列表示例（节点3的子节点）：
       [3]
        |
        v
     [39] <-> [35] <-> [38] <-+
      ^                       |
      +-----------------------+
```

这种结构的优点：
1. 插入和删除操作都是O(1)时间
2. 可以轻松遍历所有兄弟节点
3. 不需要额外的头节点

### 2.4 斐波那契堆的势函数

为了进行摊还分析，我们定义势函数：
```
Φ(H) = t(H) + 2·m(H)
```
其中：
- t(H)是堆中根列表中树的数目
- m(H)是堆中被标记的节点数目

势函数的意义：
- t(H)项反映了根列表的长度，根列表越长，潜在的合并工作越多
- m(H)项反映了被标记节点的数量，每个被标记的节点可能在未来引发级联切断操作
- 系数2是为了在摊还分析中平衡各项的贡献

## 🔧 3. 斐波那契堆的操作详解

### 3.1 创建新的斐波那契堆

```
MAKE-FIB-HEAP()
1. n[H] = 0
2. min[H] = NIL
3. return H
```

时间复杂度：O(1)

创建一个空的斐波那契堆非常简单，只需要初始化相关字段即可。

### 3.2 插入节点

```
FIB-HEAP-INSERT(H, x)
1. degree[x] = 0
2. p[x] = NIL
3. child[x] = NIL
4. mark[x] = FALSE
5. 将x插入到H的根列表中
6. if min[H] = NIL 或 key[x] < key[min[H]]
7.     min[H] = x
8. n[H] = n[H] + 1
```

时间复杂度：O(1)（实际成本和摊还成本都是O(1)）

插入操作的详细步骤：
1. 初始化新节点的各项属性
2. 将新节点添加到根列表中
3. 更新最小节点指针（如果需要）
4. 增加节点计数

**为什么插入操作如此高效？**
- 我们不做任何结构维护工作
- 新节点直接添加到根列表，不涉及复杂的比较或重排
- 最小节点的更新只是简单的比较操作

### 3.3 查找最小节点

```
FIB-HEAP-MINIMUM(H)
1. return min[H]
```

时间复杂度：O(1)

由于我们始终维护一个指向最小节点的指针，所以查找最小节点是瞬时完成的。

### 3.4 合并两个斐波那契堆

```
FIB-HEAP-UNION(H1, H2)
1. H = MAKE-FIB-HEAP()
2. min[H] = min[H1]
3. 将H2的根列表连接到H的根列表中
4. if (min[H1] = NIL) 或 (min[H2] ≠ NIL 且 key[min[H2]] < key[min[H1]])
5.     min[H] = min[H2]
6. n[H] = n[H1] + n[H2]
7. return H
```

时间复杂度：O(1)（实际成本和摊还成本都是O(1)）

合并操作的详细步骤：
1. 创建一个新的堆对象
2. 将H1的最小节点指针复制给新堆
3. 将两个根列表连接起来（通过修改指针）
4. 确定新堆的最小节点
5. 更新节点总数

**合并操作的图示：**

合并前：
```
H1: min -> [3] <-> [7] <-> [18] <-+
            ^                     |
            +---------------------+

H2: min -> [5] <-> [12] <-+
            ^              |
            +--------------+
```

合并后：
```
H:  min -> [3] <-> [7] <-> [18] <-> [5] <-> [12] <-+
            ^                                         |
            +-----------------------------------------+
```

### 3.5 抽取最小节点

这是斐波那契堆中最复杂的操作，包含以下步骤：

1. 将最小节点的所有子女添加到根列表中
2. 从根列表中删除最小节点
3. 调用CONSOLIDATE过程，确保根列表中没有两个度数相同的根
4. 更新min[H]指针

```
FIB-HEAP-EXTRACT-MIN(H)
1. z = min[H]
2. if z ≠ NIL
3.     for each child x of z
4.         将x添加到H的根列表中
5.         p[x] = NIL
6.     从H的根列表中删除z
7.     if z = right[z]
8.         min[H] = NIL
9.     else
10.        min[H] = right[z]
11.        CONSOLIDATE(H)
12.    n[H] = n[H] - 1
13.return z
```

**抽取最小节点的详细过程：**

假设我们有以下斐波那契堆：
```
min -> [3] <-> [7] <-> [18] <-+
        |                      |
        v                      |
      [39] <-> [35] <-> [38] <-+
       |       |
       v       v
     [41]     [52]
```

步骤1：将最小节点3的子节点添加到根列表
```
min -> [7] <-> [18] <-> [39] <-> [35] <-> [38] <-+
                             |    |
                             v    v
                           [41]  [52]
```

步骤2：执行CONSOLIDATE过程，合并相同度数的树

CONSOLIDATE过程：
```
CONSOLIDATE(H)
1. for i = 0 to D(n[H])
2.     A[i] = NIL
3. for each node w in the root list of H
4.     x = w
5.     d = degree[x]
6.     while A[d] ≠ NIL
7.         y = A[d]
8.         if key[x] > key[y]
9.             exchange x ↔ y
10.        FIB-HEAP-LINK(H, y, x)
11.        A[d] = NIL
12.        d = d + 1
13.    A[d] = x
14. min[H] = NIL
15. for i = 0 to D(n[H])
16.    if A[i] ≠ NIL
17.        将A[i]添加到H的根列表中
18.        if min[H] = NIL 或 key[A[i]] < key[min[H]]
19.            min[H] = A[i]
```

FIB-HEAP-LINK过程：
```
FIB-HEAP-LINK(H, y, x)
1. 从H的根列表中删除y
2. 将y作为x的子女
3. degree[x] = degree[x] + 1
```

摊还时间复杂度：O(D(n))，其中D(n) ≤ log_φ(n)，φ是黄金比例(1+√5)/2

### 3.6 减小关键字

```
FIB-HEAP-DECREASE-KEY(H, x, k)
1. if k > key[x]
2.     error "new key is greater than current key"
3. key[x] = k
4. y = p[x]
5. if y ≠ NIL 且 key[x] < key[y]
6.     CUT(H, x, y)
7.     CASCADING-CUT(H, y)
8. if key[x] < key[min[H]]
9.     min[H] = x
```

**减小关键字的详细过程：**

当我们减小一个节点的关键字时，可能会违反最小堆性质。如果节点x的新关键字小于其父节点y的关键字，我们就需要修复这种违规。

CUT过程：
```
CUT(H, x, y)
1. 从x的子女列表中删除x
2. degree[y] = degree[y] - 1
3. 将x添加到H的根列表中
4. p[x] = NIL
5. mark[x] = FALSE
```

CASCADING-CUT过程：
```
CASCADING-CUT(H, y)
1. z = p[y]
2. if z ≠ NIL
3.     if mark[y] = FALSE
4.         mark[y] = TRUE
5.     else
6.         CUT(H, y, z)
7.         CASCADING-CUT(H, z)
```

**标记机制的原理：**

标记机制是用来追踪节点结构变化的一种方法：
- 当一个节点第一次失去子节点时，我们将其标记为true
- 当一个已经被标记的节点再次失去子节点时，我们将它也切断并放到根列表中
- 这样可以防止树结构变得过于不平衡

### 3.7 删除节点

```
FIB-HEAP-DELETE(H, x)
1. FIB-HEAP-DECREASE-KEY(H, x, -∞)
2. FIB-HEAP-EXTRACT-MIN(H)
```

删除操作通过将节点的关键字减小到负无穷，使其成为最小节点，然后抽取最小节点来实现。

摊还时间复杂度：O(D(n))

## 📊 4. 时间复杂度详细分析

### 4.1 最大度数的界限

为了分析抽取最小节点操作的时间复杂度，我们需要确定任意节点的最大度数。

**定理**：斐波那契堆中任意节点x的度数D(n)最多为O(log n)。

**证明思路：**
1. 用斐波那契数列来界定节点的最小大小
2. 证明度数为k的节点至少包含F_{k+2}个后代节点
3. 利用斐波那契数列的增长性质得出度数上界

设s_k表示斐波那契堆中度数为k的任意节点x的子孙节点数（包括x本身）的最小可能值。

**引理1**：对于所有整数k≥0，有s_k ≥ F_{k+2}，其中F_k是第k个斐波那契数。

**引理2**：对于所有整数k≥0，有F_k ≥ φ^k / √5，其中φ = (1+√5)/2 ≈ 1.618。

结合这两个引理，我们可以得到：
s_k ≥ φ^k / √5

由于任意节点的子孙节点数最多为n，我们有：
n ≥ s_k ≥ φ^k / √5

解这个不等式得到：
k ≤ log_φ(√5·n) = O(log n)

因此，D(n) = O(log n)。

### 4.2 摊还分析

使用势函数Φ(H) = t(H) + 2·m(H)进行摊还分析：

#### 4.2.1 INSERT操作的摊还成本

实际成本：O(1)
势的变化：t(H)增加1，m(H)不变
摊还成本：O(1) + 1 = O(1)

#### 4.2.2 EXTRACT-MIN操作的摊还成本

实际成本：O(D(n) + t(H))
势的变化：t(H)可能减少到D(n)+1，m(H)不变
摊还成本：O(D(n) + t(H)) - t(H) + D(n) + 1 = O(D(n)) = O(log n)

#### 4.2.3 DECREASE-KEY操作的摊还成本

实际成本：O(c)，其中c是级联切断的次数
势的变化：t(H)增加c，m(H)减少最多c-1
摊还成本：O(c) + c - (c-1) = O(1)

## 💡 5. 斐波那契堆的优势

### 5.1 摊还效率

斐波那契堆最重要的特点是其优秀的摊还时间复杂度：

1. **插入、合并操作**：O(1)摊还时间
2. **查找最小**：O(1)实际时间
3. **抽取最小、删除**：O(log n)摊还时间
4. **减小关键字**：O(1)摊还时间

### 5.2 懒惰操作

斐波那契堆采用懒惰策略，将昂贵的操作推迟到必要时才执行：

1. **延迟合并**：不像二项堆那样立即维护树的结构，而是在抽取最小节点时才进行合并
2. **标记机制**：通过标记位跟踪节点的结构变化，优化减小关键字操作

### 5.3 理论意义

斐波那契堆在理论上具有重要意义：

1. **接近下界**：许多操作达到了理论下界
2. **启发作用**：启发了其他数据结构的设计

## 🧪 6. 示例代码解析

我们的示例代码展示了斐波那契堆的核心概念：

### 6.1 数据结构设计

1. **[FibonacciNode](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L12-L26) 结构**：表示斐波那契堆的节点
2. **[FibonacciHeap](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L29-L42) 类**：表示整个斐波那契堆

### 6.2 核心操作实现

1. **[insert](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L52-L64)**：插入节点到根列表
2. **[extractMin](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L70-L97)**：抽取最小节点并维护堆性质
3. **[unite](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L126-L153)**：合并两个斐波那契堆
4. **[decreaseKey](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L156-L176)**：减小节点的关键字值
5. **[deleteNode](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L207-L215)**：删除指定节点

### 6.3 辅助函数

1. **[consolidate](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L109-L124)**：维护根列表中没有相同度数的树
2. **[cut](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L178-L196)**：切断父子关系
3. **[cascadingCut](file://C:\Users\HaPpY\Desktop\study_Introduction-to-Algorithms\C5\U19\FIBONACCI-HEAP\FibonacciHeap.cpp#L198-L205)**：级联切断操作

## 📈 7. 斐波那契堆与其他堆的比较

### 7.1 详细比较表

| 特性 | 二叉堆 | 二项堆 | 斐波那契堆 |
|------|--------|--------|------------|
| 创建 | O(1) | O(1) | O(1) |
| 插入 | O(log n) | O(log n) | O(1) |
| 查找最小 | O(1) | O(log n) | O(1) |
| 抽取最小 | O(log n) | O(log n) | O(log n) |
| 合并 | O(n) | O(log n) | O(1) |
| 减小关键字 | O(log n) | O(log n) | O(1) |
| 删除 | O(log n) | O(log n) | O(log n) |

### 7.2 适用场景分析

#### 7.2.1 二叉堆
- **优点**：实现简单，常数因子小
- **缺点**：合并操作效率低
- **适用场景**：不需要频繁合并的优先队列应用

#### 7.2.2 二项堆
- **优点**：支持高效的合并操作
- **缺点**：所有操作都需要维护严格的结构
- **适用场景**：需要频繁合并但不需要频繁减小关键字的应用

#### 7.2.3 斐波那契堆
- **优点**：插入、合并、减小关键字操作都非常高效
- **缺点**：实现复杂，常数因子大，实际性能可能不如理论分析
- **适用场景**：大规模数据、需要大量插入和合并操作的图算法

## 🎯 8. 应用场景详解

### 8.1 图算法中的应用

#### 8.1.1 Dijkstra最短路径算法

传统的Dijkstra算法使用二叉堆实现时的时间复杂度为O((V+E)log V)。

使用斐波那契堆优化后：
- INSERT操作：O(1)
- DECREASE-KEY操作：O(1)
- EXTRACT-MIN操作：O(log V)

总时间复杂度优化为O(V log V + E)。

这对于边数远大于顶点数的稠密图来说是一个显著的改进。

#### 8.1.2 Prim最小生成树算法

Prim算法与Dijkstra算法类似，也可以通过斐波那契堆优化：
- 时间复杂度从O((V+E)log V)优化到O(V log V + E)

### 8.2 其他应用

1. **网络流算法**：在网络流算法中作为优先队列使用
2. **贪心算法**：在需要频繁插入和查找最小元素的贪心算法中
3. **调度算法**：在操作系统或任务调度中的优先级队列

## ⚠️ 9. 实现注意事项

### 9.1 内存管理

斐波那契堆涉及大量动态内存分配，需要注意：
- 及时释放不再使用的节点
- 避免内存泄漏
- 考虑使用内存池优化频繁的内存分配

### 9.2 实际性能

虽然斐波那契堆在理论上具有很好的摊还时间复杂度，但在实际应用中：
- 常数因子较大，对于小规模数据可能不如简单数据结构
- 需要额外的存储空间保存标记位和其他指针
- 实现复杂度较高，容易出错

### 9.3 适用场景

斐波那契堆最适合以下场景：
- 需要大量插入和合并操作
- 数据规模较大
- 对渐近时间复杂度敏感而非对常数因子敏感

## 🧠 10. 总结

斐波那契堆是一种理论和实践都很有趣的数据结构，它展示了如何通过懒惰操作和摊还分析来优化数据结构的性能。

### 10.1 核心思想回顾

1. **懒惰操作**：推迟昂贵的操作直到必要时才执行
2. **摊还分析**：通过势能法分析操作序列的整体性能
3. **结构优化**：通过巧妙的数据结构设计优化特定操作

### 10.2 关键创新点

1. **延迟合并**：不急于维护树结构的一致性，而是等到必须的时候再做
2. **标记机制**：通过标记节点来追踪结构变化，优化减小关键字操作
3. **势函数设计**：精心设计的势函数使得摊还分析简洁明了

### 10.3 学习价值

1. **算法思维**：学习如何通过延迟工作来优化性能
2. **摊还分析**：掌握一种重要的算法分析技术
3. **数据结构设计**：理解高级数据结构的设计原则

### 10.4 实践建议

1. **动手实现**：通过实际编码加深对斐波那契堆操作的理解
2. **性能测试**：对比不同堆结构在实际应用中的性能表现
3. **应用场景分析**：研究斐波那契堆在图算法中的应用

通过我们的示例代码，您可以清楚地看到斐波那契堆的各种操作是如何实现的，包括插入、抽取最小节点、减小关键字等核心功能。这为深入学习和实践提供了坚实的基础。